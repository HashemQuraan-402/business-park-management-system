export const environment = {
  production: false,
  // Matches the "https" launch profile in Properties/launchSettings.json.
  // If your browser rejects the self-signed dev certificate, either open
  // https://localhost:7153 once and accept it, or switch this to the http
  // profile: 'http://localhost:5145/api'
  apiBaseUrl: 'https://localhost:7153/api'
};
